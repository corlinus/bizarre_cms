%p
  Link to this page
  = link_to @page.title, page_path(@page)

%h3 Page children
%ul
  - @page.children.each do |sub_page|
    = link_to sub_page.title, page_path(sub_page)

%h3 Page summary (content preview) 
= raw @page.summary

%h3 Page content
= raw @page.body

